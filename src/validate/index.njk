---
layout: layouts/base.njk
title: Validate
---
<h1>Validate</h1>
<ol>
  <li>Check your <strong>Valiterm script</strong> is valid JSON (well-formed, quoted keys/values).</li>
  <li>Check your <strong>DefinedTerm JSON-LD</strong> passes a structured data/JSON-LD validator.</li>
  <li>Ensure <code>@id</code>, <code>url</code>, and <code>inDefinedTermSet</code> are correct/canonical.</li>
  <li>If your term is an alias of an existing canonical term, link it via aliases and keep a scope note.</li>
</ol>
<p>When submission opens, you’ll be able to upload your JSON or send a PR to add it to the public set.</p>

